# ME3302-Project

## 1. Generate the Dataset  
Before running the model, generate the dataset running the dataset_generation.py script. The model has been trained on this type of dataset.

## 2: Run the model on any generated test data 
Execute the following code: python3 model.py to see how the model performs for the test image present in the testdata directory.
The image present in the testData folder is a defeced images and its label is 0.



